# proxy-server

## Features

* [x] create a HTTP server, which proxy all requests
* [x] support HTTPS protocol in passed URL
* [ ] caching resource in local directory
* [ ] check if resource is cached in local directory, and use it instead of make a request
* [ ] support denylist of URLs which will be not cached

## Usage

```bash
proxy-server --url URL
```
